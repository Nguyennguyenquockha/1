local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- Create GUI
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "KhaGetKeyGui"
ScreenGui.ResetOnSpawn = false

-- Main Frame
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 400, 0, 220)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -110)
MainFrame.BackgroundTransparency = 0.4
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame:TweenSize(UDim2.new(0, 400, 0, 220), "Out", "Bounce", 0.5)

-- UICorner
local corner = Instance.new("UICorner", MainFrame)
corner.CornerRadius = UDim.new(0, 12)

-- Background image
local bgImage = Instance.new("ImageLabel", MainFrame)
bgImage.Size = UDim2.new(1, 0, 1, 0)
bgImage.Position = UDim2.new(0, 0, 0, 0)
bgImage.Image = "rbxassetid://138057999099280"
bgImage.BackgroundTransparency = 0.5
bgImage.ImageTransparency = 0.5

-- Title
local title = Instance.new("TextLabel", MainFrame)
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "🔑Get Key system"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 24

-- Close Button
local close = Instance.new("TextButton", MainFrame)
close.Size = UDim2.new(0, 30, 0, 30)
close.Position = UDim2.new(1, -35, 0, 5)
close.Text = "✖"
close.TextColor3 = Color3.new(1, 0.4, 0.4)
close.BackgroundTransparency = 1
close.TextSize = 18
close.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- Input KeyBox
local KeyBox = Instance.new("TextBox", MainFrame)
KeyBox.Size = UDim2.new(0, 250, 0, 35)
KeyBox.Position = UDim2.new(0.05, 0, 0.35, 0)
KeyBox.PlaceholderText = "Nhập key tại đây..."
KeyBox.Text = ""
KeyBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
KeyBox.TextColor3 = Color3.new(1, 1, 1)
KeyBox.Font = Enum.Font.Gotham
KeyBox.TextSize = 16
Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0, 6)

-- Get Key Button
local GetKey = Instance.new("TextButton", MainFrame)
GetKey.Size = UDim2.new(0, 100, 0, 35)
GetKey.Position = UDim2.new(0.05, 0, 0.7, 0)
GetKey.Text = "🌐 Get Key"
GetKey.BackgroundColor3 = Color3.fromRGB(60, 100, 255)
GetKey.TextColor3 = Color3.new(1,1,1)
GetKey.Font = Enum.Font.Gotham
GetKey.TextSize = 15
Instance.new("UICorner", GetKey).CornerRadius = UDim.new(0, 6)

GetKey.MouseButton1Click:Connect(function()
	setclipboard("https://pastefy.app/bzBj4H06")
	GetKey.Text = "📋 Copied!"
	wait(1)
	GetKey.Text = "🌐 Get Key"
end)

-- Check Key Button
local CheckKey = Instance.new("TextButton", MainFrame)
CheckKey.Size = UDim2.new(0, 100, 0, 35)
CheckKey.Position = UDim2.new(0.65, 0, 0.7, 0)
CheckKey.Text = "✅ Check Key"
CheckKey.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
CheckKey.TextColor3 = Color3.new(1,1,1)
CheckKey.Font = Enum.Font.Gotham
CheckKey.TextSize = 15
Instance.new("UICorner", CheckKey).CornerRadius = UDim.new(0, 6)

-- Notification Label
local Notify = Instance.new("TextLabel", MainFrame)
Notify.Size = UDim2.new(0, 0, 0, 40)
Notify.Position = UDim2.new(-1, 0, 0.5, -20)
Notify.BackgroundTransparency = 1
Notify.Text = "✅ Key chính xác! Đang mở Hub..."
Notify.TextColor3 = Color3.fromRGB(0, 255, 127)
Notify.Font = Enum.Font.GothamBold
Notify.TextScaled = true
Notify.Visible = false

-- Key system (24h)
local DataStoreKey = "KhaHubKeyTime"
local CorrectKey = "KHADEPTRAIVAILON100"

-- Save time key used
local function saveKeyTime()
	if isfile and writefile then
		writefile(DataStoreKey, tostring(os.time()))
	end
end

-- Load time key used
local function loadKeyTime()
	if isfile and readfile and isfile(DataStoreKey) then
		return tonumber(readfile(DataStoreKey))
	end
	return nil
end

-- Check Key
CheckKey.MouseButton1Click:Connect(function()
	if KeyBox.Text == CorrectKey then
		Notify.Visible = true
		Notify.Position = UDim2.new(-1, 0, 0.2, 0)
		Notify.Size = UDim2.new(0, 0, 0, 40)

		local tween = TweenService:Create(Notify, TweenInfo.new(0.5, Enum.EasingStyle.Back), {
			Position = UDim2.new(0.1, 0, 0.2, 0),
			Size = UDim2.new(0.8, 0, 0, 40)
		})
		tween:Play()

		saveKeyTime()
		wait(2)
		ScreenGui:Destroy()
		-- Load main hub here
		loadstring(game:HttpGet('https://raw.githubusercontent.com/Nguyennguyenquockha/1/refs/heads/main/boost%20speed_script.txt'))()
	else
		KeyBox.Text = ""
		KeyBox.PlaceholderText = "❌ Sai key, thử lại!"
	end
end)

-- Auto check if key was saved within 24h
local lastUsed = loadKeyTime()
if lastUsed and os.time() - lastUsed < 86400 then
	ScreenGui:Destroy()
	loadstring(game:HttpGet('https://raw.githubusercontent.com/Nguyennguyenquockha/1/refs/heads/main/boost%20speed_script.txt'))()
end